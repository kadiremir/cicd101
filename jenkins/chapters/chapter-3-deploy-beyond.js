/* ============================================================
   Chapter 3 — Deploy & Beyond  (placeholder)
   ------------------------------------------------------------
   Self-registers into window.CHAPTERS via registerChapter().
   levels:[] renders as "Coming soon" on the chapter map.
   Add level objects here to make it playable — see campaign-data.js
   for the LEVEL shape reference.
   ============================================================ */
registerChapter({
  id: 3,
  title: "Deploy & Beyond",
  summary: "Environments, approvals and rollouts. Drop new levels here to keep the campaign growing.",
  accent: "blue",
  levels: []   // empty → shows as “Coming soon” on the map
});
